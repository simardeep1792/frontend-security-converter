# Note: metadataByDomain query no longer exists in the new schema
# Using dataObjectCountsByMetadataDomain to get domain counts
query MetadataByDomain($domain: String!) {
  dataObjectCountsByMetadataDomain(domain: $domain) {
    domain
    count
  }
}
