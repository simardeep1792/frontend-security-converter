# Note: searchMetadataByTag query no longer exists in the new schema
# Using dataObjectsByTitle as alternative search
query SearchMetadataByTag($pattern: String!) {
  dataObjectsByTitle(title: $pattern) {
    id
    title
    description
    createdAt
    metadata {
      id
      domain
      tags
    }
  }
}
