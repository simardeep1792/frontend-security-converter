# Note: metadataByTag query no longer exists in the new schema
# Using dataObjectsByTitle as alternative search
query MetadataByTag($tag: String!) {
  dataObjectsByTitle(title: $tag) {
    id
    title
    description
    createdAt
    metadata {
      id
      domain
      tags
    }
  }
}
