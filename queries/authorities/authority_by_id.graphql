query AuthorityById($id: UUID!) {
  authorityById(id: $id) {
    id
    nationId
    name
    email
    phone
    createdAt
    updatedAt
    expiresAt
    nation {
      id
      nationName
      nationCode
    }
  }
}